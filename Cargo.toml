# Корень workspace — объединяет крейты проекта.
# Сам по себе кода не содержит, только перечисляет участников.
[workspace]
resolver = "2"
members = [
    "core",
    "server",
    "client/src-tauri",
]

# Общие настройки версий/метаданных можно вынести сюда позже (workspace.package).
